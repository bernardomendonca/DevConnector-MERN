const isEmpty = value =>
  value === undefined ||
  value === null ||
  (typeof value === "object" && Object.keys(value).length === 0) ||
  (typeof value === "string" && value.trim().length === 0);
// this is an specialized isEmpty function, in opposal to the VALIDATOR isEmpty

export default isEmpty;
